# PostgreSQL password
# Set the same value here and nowhere else — docker-compose passes it to all services.
POSTGRES_PASSWORD=change_me

# Access the UI at: http://192.168.x.x:8080
# Replace 192.168.x.x with your NAS IP address.
